enum Roles {
    USER = 1,
    DRIVER = 2,
    OPERATOR = 3,
    ADMIN = 4
}
enum VehicleType {
    CAR = 1,
    BIKE = 2
}

enum DriverStatus{
    AVAILABLE = 1,
    BUSY = 2,
}

export { Roles, VehicleType, DriverStatus };